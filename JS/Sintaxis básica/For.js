// Sentencia que permite ejecutar un bloque de instrucciones un determinado numero de veces.
for (let a=0; a<5 ; a++){
    console.log(a);
}
// El forEach ejecuta una función recibida sobre cada elemento de el arreglo.
let array = [1,2,3,4,5];
array.forEach((Element)=>console.log(Element));